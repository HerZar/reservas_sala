<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.com.eduit.reservassala">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_reservas"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_reservas_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".MainActivity"
            android:label="@string/label_reservas"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AgregarReserva"
            android:label="@string/label_nueva_reserva"
                  />
        <activity
            android:name=".AgendaResHist"
            android:label="@string/label_historico_reservas"
            />
        <activity
            android:name=".SetingsActivity"
            android:label="@string/label_administracion"
            />
    </application>

</manifest>